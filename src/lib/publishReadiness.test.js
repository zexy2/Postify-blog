import {describe,expect,it} from 'vitest';import {getPublishReadiness} from './publishReadiness';describe('getPublishReadiness',()=>{it('keeps required separate from advisory',()=>{const r=getPublishReadiness({title:'Useful title',body:'x'.repeat(200)});expect(r.ready).toBe(true);expect(r.score).toBe(67)});it('recognizes rich structure',()=>expect(getPublishReadiness({title:'Practical guide',body:'x'.repeat(200),bodyHtml:'<h2>Steps</h2>'}).score).toBe(100));it('rejects missing core',()=>expect(getPublishReadiness({title:'Tiny',body:'short'}).ready).toBe(false))});
