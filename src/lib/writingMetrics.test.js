import {describe,expect,it} from 'vitest';import {getWritingMetrics} from './writingMetrics';describe('getWritingMetrics',()=>{it('empty',()=>expect(getWritingMetrics('')).toEqual({characters:0,words:0,readingMinutes:0}));it('counts words',()=>expect(getWritingMetrics('one two\nthree').words).toBe(3));it('rounds up',()=>expect(getWritingMetrics(Array(221).fill('word').join(' ')).readingMinutes).toBe(2))});
