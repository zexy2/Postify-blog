import { describe,expect,it } from 'vitest';import { formatPostDate,getPostPresentation,getPostType } from './postPresentation';describe('postPresentation',()=>{it('uses explicit type',()=>expect(getPostType({contentType:'decision'})).toBe('decision'));it('derives guide',()=>expect(getPostType({category:'Frontend',title:'Performans rehberi'})).toBe('guide'));it('invalid dates empty',()=>expect(formatPostDate('not-a-date','tr')).toBe(''));it('update preferred',()=>expect(getPostPresentation({publishedAt:'2026-08-01T10:00:00.000Z',updatedAt:'2026-08-03T10:00:00.000Z'},'tr').dateLabel).toBe('Son güncelleme'))});
