# Verified Knowledge production apply

Migration: `migrations/202608280900_verified_knowledge.sql`

Preconditions:
- Existing 20260807 base/content/security migrations are already present.
- Take a schema backup/snapshot in Supabase before applying.
- Apply through Supabase CLI/SQL Editor using an authenticated project owner connection; never expose a service-role/database password to the browser app.

Verification immediately after apply:
1. Run `verify-readonly.sql`.
2. Confirm `post_evidence_summary` is anonymously readable but `post_confirmations` is not anonymously writable.
3. Publish/update one authenticated author post with evidence fields.
4. From a different authenticated user, add one confirmation; verify the author cannot self-confirm.
5. Confirm `/knowledge/<slug>.<locale>.json` is generated by the next deploy.

Rollback:
- Prefer frontend rollback first. Migration is additive and leaving empty new tables/columns is safer than destructive rollback.
- See `verified-knowledge-rollback.md` before any destructive DB rollback.
